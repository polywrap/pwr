#import * into Context from "https/http.wrappers.dev/u/test/invocation-context"

type Module {
  set(key: String!, value: Bytes!): Boolean!
  get(key: String!): Bytes
  remove(key: String!): Boolean!
  has(key: String!): Boolean!
  keys: [String!]!
  values: [Bytes!]!
  entries: [KeyValuePair!]!
}

type KeyValuePair {
  key: String!
  value: Bytes!
}
